#include <stdio.h>

int main(){
    int a[4][2], b[4][2];
    for (int l=0;l<4;l++){
        for(int c=0;c<2;c++){
            printf("Digite o valor do espaço [%d][%d]",l,c);
            scanf("%d", a[l][c]);
        }
    }
    for (int i=)

    return 0;
}